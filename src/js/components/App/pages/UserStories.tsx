import * as React from 'react';

import Container from '../../reusable/Container';

const UserStories = () => (
  <Container.FlexCols className="gap-8"></Container.FlexCols>
);

export default UserStories;
